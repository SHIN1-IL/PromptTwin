/** Must match package.json → contributes.views → prompttwin → id */
export const PROMPTTWIN_VIEW_ID = 'prompttwin.mainView';

/** Must match package.json → contributes.viewsContainers.activitybar → id */
export const PROMPTTWIN_VIEW_CONTAINER_ID = 'prompttwin';
